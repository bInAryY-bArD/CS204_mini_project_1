#include<bits/stdc++.h>
using namespace std;


int main(){

    string SB_opcode, ins, r1, r2, label;

                            //Vector SB stores the name of all the SB instructions that our Compiler can detect
    vector <string> SB, SBfunc3;
    SB = {"beq", "bne", "bge", "blt"};
    SB_opcode = "1100011";
    SBfunc3 = {"000", "001", "101", "100"};

    if(find(SB.begin(), SB.end(), ins) != SB.end()){
        // iss >> r1;
        // iss >> r2;
        // iss >> label

        


    }
    

}